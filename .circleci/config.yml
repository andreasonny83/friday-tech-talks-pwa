version: 2
jobs:
  build:
    docker:
      - image: cypress/base:6
        environment:
          ## this enables colors in the output
          TERM: xterm
    steps:
      - checkout
      - run: npm install
      - run: npm run e2e:ci
